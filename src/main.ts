import { Plugin } from "obsidian";

export default class ObsidianAIPlugin extends Plugin {
  public async onload(): Promise<void> {
    // Intentionally minimal for scaffold story.
  }

  public onunload(): void {
    // Intentionally minimal for scaffold story.
  }
}
